# Skills repo

Skill definitions live under `skills/local/`. The user installs them later. The install command is in `README.md`.

## Edit a skill

Update `skills/local/<name>/` only. Leave `~/.agents`, `~/.claude/skills`, and any other install directory unchanged, and do not run the skills installer.

**Done when:** the definition in this repo is updated and no install location was written.
